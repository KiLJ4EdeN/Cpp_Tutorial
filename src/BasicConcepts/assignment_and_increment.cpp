#include <iostream>
using namespace std;

int main() 
{
  // Assignment Operations:
  x += 3; // equivalent to x = x + 3
  x *= 3; // equivalent to x = x * 3
  x /= 2; // equivalent to x = x / 2
  x %= 4; // equivalent to x = x % 4
  
  x++; // equivalent to x = x + 1
  x--; // equivalant to x = x - 1
  
  return 0;
}
