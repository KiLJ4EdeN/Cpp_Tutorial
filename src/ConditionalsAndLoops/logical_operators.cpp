#include <iostream>
using namespace std;

int main()
{
	// && (and)
	// || (or)
	// ! (not)
    int age = 10;
    if ( !(age > 16) ) {
        cout << "Your age is less than 16" << endl;
    }

    return 0;
}